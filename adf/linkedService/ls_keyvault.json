{
    "name": "ls_keyvault",
    "type": "Microsoft.DataFactory/factories/linkedservices",
    "properties": {
        "type": "AzureKeyVault",
        "typeProperties": {
            "baseUrl": "https://<keyvault_name>.vault.azure.net/"
        },
        "annotations": ["DEPRECATED"],
        "description": "DEPRECATED — Key Vault removed. ADF linked services use SecureString (ADF encrypts at publish). Databricks notebooks use dbutils.secrets.get(scope='hpe-forecast', key='...'). No Key Vault resource needed."
    }
}
