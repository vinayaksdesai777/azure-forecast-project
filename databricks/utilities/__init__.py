# Marks utilities/ as a regular package.
# Without this, Python treats the directory as a namespace package, which does not
# reliably resolve submodules when the repo is synced through Databricks Repos.
